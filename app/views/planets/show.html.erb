<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <title>Bootstrap Utility Classes</title>
    <link rel="shortcut icon" href="data:image/x-icon;" type="image/x-icon">

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
   <link rel="stylesheet" href="https://use.fontawesome.com/releases/v6.1.2/css/all.css">
  </head>
<%# Now we need the top banner, title and symbol %>

    <!-- navbar -->
    <%# now we do the pic %>
    <div class="header-container d-flex align-items-center">
     <div class="image-container">
         <%= image_tag "logo-red.png", style:"width:80px", alt: "logo-image"  %>
      </div>
              <!-- Now we do the title -->
      <div class="title-container">
        <h1> <%= @planet.name %></h1>
      </div>
    </div>
    <!-- /navbar -->
<%# Now lets do the image %>
<div class = "container">
  <div class="container-top-btn">
    <div>
    <%= link_to "/planets" do %>
        <i class="fa-solid fa-chevron-left button-round"></i>
      <% end %></div>
  </div>
    <%= image_tag "pexels.jpg", alt: "planet-image", class: "show-img" %>
</div>

<div class = "container-description">
  <div class = "title">
    <h2>Description</h2>
    <h3><%="⭐" * @planet.rating %></h3>
  </div>
  <div class= "content">
    <p><%= @planet.description * 8%></p>
  </div>
    <hr>
</div>
<div class = "container-offers">
  <div class = "title">
    <h3>What this planet offers</h3>
  </div>
<div class="icons">
  <div class = "pop">
    <i class="fa-solid fa-people-group"></i>
    <p><%= @planet.population %> people</p>
  </div>
  <div class = "size">
      <i class="fa-solid fa-ruler-horizontal"></i>
      <p><%= @planet.size %> km</p>
  </div>
</div>
<hr>
<div class = "container-price">
  <div class = "title">
    <h3>Price per night</h3>
  </div>
<p>🪙<%= @planet.price %></p>
</div>

  <div class="btn-container">
    <%= link_to "Book now", new_planet_booking_path(@planet), class: "button-primary" %>
  </div>

  <div class="booking-list">
    <% @planet.bookings.each do |booking|%>
      <p><%= booking.comment %>: <%= booking.date_start %> - <%= booking.date_end %></p>
    <% end%>
  </div>
</div>

</div>
